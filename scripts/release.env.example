# 本地发版连接配置模板。
# 用法：复制为 scripts/.release.env（已被 .gitignore 忽略，不会入库），按盒子实际填写。
#   cp scripts/release.env.example scripts/.release.env
#
# 仅 local / rollback / status / logs 子命令需要这些 SSH 参数；
# 默认的 'pnpm deploy'（走 CI）只需 gh 已登录，不读本文件。
#
# 与 GitHub Secrets 的对应关系（CI 侧用的是同名概念，但 key 走密钥内容，本地走密钥文件）：
#   DEPLOY_HOST     <- secrets.DEPLOY_HOST
#   DEPLOY_USER     <- secrets.DEPLOY_USER
#   DEPLOY_PORT     <- secrets.DEPLOY_PORT
#   DEPLOY_IDENTITY <- 本地私钥文件路径（CI 侧是 secrets.DEPLOY_SSH_KEY 的内容）

# 盒子地址（必需）
DEPLOY_HOST=
# SSH 用户（必需，例如 root）
DEPLOY_USER=root
# SSH 端口（默认 22）
DEPLOY_PORT=22
# 私钥文件路径（可选；留空走 ssh-agent / 默认 key）
DEPLOY_IDENTITY=

# 盒子部署根（默认 /opt/deploy-management，一般无需改）
# DEPLOY_ROOT=/opt/deploy-management
# CI 部署分支（默认 main）
# DEPLOY_BRANCH=main
# workflow 文件名（默认 deploy.yml）
# DEPLOY_WORKFLOW=deploy.yml
