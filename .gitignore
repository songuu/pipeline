.worktrees/
.agent-runs/
*.bak.*
.env
.env.*
!.env.example
.deploy-data/
node_modules/
.next/
dist/
apps/api/dist
# 构建产物不入库：CI 是唯一生产者，避免机上 build 弄脏工作区导致 git pull --ff-only abort
next-env.d.ts
*.log
.handoff/
# 本地发版 staging 目录（deploy:local / deploy-prod.ps1 的临时构建产物）
.codex-tmp/
# 本地发版连接配置（含 SSH host/user，绝不入库；模板见 scripts/release.env.example）
/scripts/.release.env